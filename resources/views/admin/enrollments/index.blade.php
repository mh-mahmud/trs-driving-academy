@extends('layouts.admin')
@section('title','Enrollments') @section('page_title','Enrollments')
@section('content')
<div class="page-heading mb-4"><h1>Offline Course Enrollments</h1><p>Review and manage submitted student applications.</p></div>
@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif
<div class="panel"><div class="settings-body"><form class="row g-2 mb-4"><div class="col-md-6"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search name, mobile or email"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['pending','approved','rejected','completed'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-md-3 d-flex gap-2"><button class="btn btn-admin-primary flex-fill">Filter</button><a class="btn btn-light border" href="{{ route('admin.enrollments.index') }}">Reset</a></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Student</th><th>Course</th><th>Branch</th><th>Fee</th><th>Date</th><th>Status</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($enrollments as $item)<tr><td><strong>{{ $item->name }}</strong><small class="d-block text-secondary">{{ $item->mobile }} · {{ $item->email }}</small></td><td>{{ $item->course?->title }}</td><td>{{ $item->branch?->name }}</td><td>BDT {{ number_format($item->payable_amount) }}</td><td>{{ $item->created_at->format('d M Y') }}</td><td><span class="badge {{ ['approved'=>'text-bg-success','rejected'=>'text-bg-danger','completed'=>'text-bg-primary'][$item->status]??'text-bg-warning' }}">{{ ucfirst($item->status) }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.enrollments.show',$item) }}"><i class="bi bi-eye"></i></a><form class="d-inline" method="POST" action="{{ route('admin.enrollments.destroy',$item) }}" onsubmit="return confirm('Delete this enrollment?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-secondary">No enrollment found.</td></tr>@endforelse</tbody></table></div>{{ $enrollments->links() }}</div></div>
@endsection
