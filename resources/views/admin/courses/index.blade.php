@extends('layouts.admin')
@section('title','Course List')
@section('page_title','Course List')
@section('content')
<div class="page-heading d-flex justify-content-between align-items-center mb-4"><div><h1>Course List</h1><p>All courses available in the system.</p></div><a class="btn btn-admin-primary" href="{{ route('admin.courses.create') }}"><i class="bi bi-plus-lg me-1"></i>Add Course</a></div>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<div class="panel"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Course</th><th>Slug</th><th>Type</th><th>City</th><th>Branch</th><th>Fee</th><th>Status</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($courses as $course)<tr><td><div class="course-table-title">@if($course->image)<img src="{{ asset('storage/'.$course->image) }}" alt="{{ $course->title }}" loading="lazy">@else<span class="course-image-placeholder"><i class="bi bi-image"></i></span>@endif<strong>{{ $course->title }}</strong></div></td><td><code>{{ $course->slug }}</code></td><td>{{ $course->type->name }}</td><td>{{ $course->city->name }}</td><td>{{ $course->branch->name }}</td><td>৳{{ number_format($course->fee,2) }}</td><td><span class="status-pill">{{ $course->is_active?'Active':'Inactive' }}</span></td><td class="text-end"><a class="btn btn-sm btn-light border" href="{{ route('admin.courses.edit',$course) }}" title="Edit"><i class="bi bi-pencil"></i></a><form class="d-inline" method="POST" action="{{ route('admin.courses.destroy',$course) }}" onsubmit="return confirm('Are you sure you want to delete this course?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" title="Delete"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="8" class="text-center py-5 text-secondary">No courses added yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $courses->links() }}</div>
@endsection
