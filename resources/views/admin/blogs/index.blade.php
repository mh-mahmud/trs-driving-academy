@extends('layouts.admin')
@section('title','Blog List')
@section('page_title','Blog List')
@section('content')
<div class="page-heading d-flex justify-content-between align-items-center mb-4"><div><h1>Blog List</h1><p>Manage all blog posts.</p></div><a class="btn btn-admin-primary" href="{{ route('admin.blogs.create') }}"><i class="bi bi-plus-lg"></i> Create Blog</a></div>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<div class="panel"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Blog</th><th>Slug</th><th>Category</th><th>Status</th><th>Published</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($blogs as $blog)<tr><td><div class="course-table-title">@if($blog->image)<img src="{{ asset('storage/'.$blog->image) }}" alt="{{ $blog->title }}">@else<span class="course-image-placeholder"><i class="bi bi-image"></i></span>@endif<strong>{{ $blog->title }}</strong></div></td><td><code>{{ $blog->slug }}</code></td><td>{{ $blog->category->name }}</td><td><span class="status-pill">{{ ucfirst($blog->status) }}</span></td><td>{{ $blog->published_at?->format('d M Y')?:'—' }}</td><td class="text-end"><a class="btn btn-sm btn-light border" href="{{ route('admin.blogs.edit',$blog) }}"><i class="bi bi-pencil"></i></a><form class="d-inline" method="POST" action="{{ route('admin.blogs.destroy',$blog) }}" onsubmit="return confirm('Delete this blog?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5">No blogs found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $blogs->links() }}</div>
@endsection
