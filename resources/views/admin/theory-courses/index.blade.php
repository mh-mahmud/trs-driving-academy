@extends('layouts.admin')
@section('title','Theory Courses') @section('page_title','Theory Courses')
@section('content')<div class="page-heading d-flex justify-content-between mb-4"><div><h1>Theory Course List</h1><p>Manage theory courses and public page content.</p></div><a class="btn btn-admin-primary" href="{{ route('admin.theory-courses.create') }}"><i class="bi bi-plus-lg"></i> Add Course</a></div>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif @if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<form class="panel settings-panel mb-4" method="POST" action="{{ route('admin.theory-courses.settings') }}" enctype="multipart/form-data">@csrf @method('PUT')<div class="panel-head"><h3>Page Settings</h3></div><div class="settings-body row g-3"><div class="col-md-4"><label class="form-label">Page Title</label><input class="form-control" name="page_title" value="{{ $settings->page_title?:'Theory Courses' }}" required></div><div class="col-md-4"><label class="form-label">Category Title</label><input class="form-control" name="category_title" value="{{ $settings->category_title?:'Course Category' }}" required></div><div class="col-md-4"><label class="form-label">FAQ Title</label><input class="form-control" name="faq_title" value="{{ $settings->faq_title?:'Frequently Asked Questions' }}" required></div><div class="col-12"><label class="form-label">FAQ Image</label><input class="form-control" type="file" name="faq_image" accept="image/*"></div><div class="col-12 text-end"><button class="btn btn-admin-primary">Save Settings</button></div></div></form>
<div class="panel table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Image</th><th>Course</th><th>Type</th><th>City / Branch</th><th>Fee</th><th>Status</th><th></th></tr></thead><tbody>@forelse($courses as $course)<tr><td>@if($course->image)<img src="{{ asset('storage/'.$course->image) }}" style="width:70px;height:48px;object-fit:cover;border-radius:6px" alt="">@endif</td><td><strong>{{ $course->title }}</strong><small class="d-block text-secondary">{{ $course->slug }}</small></td><td>{{ $course->type->name }}</td><td>{{ $course->city->name }} / {{ $course->branch->name }}</td><td>৳{{ number_format($course->fee) }}</td><td>{{ $course->is_active?'Active':'Inactive' }}</td><td><div class="d-flex gap-1"><a class="btn btn-sm btn-light border" href="{{ route('admin.theory-courses.edit',$course) }}"><i class="bi bi-pencil"></i></a><form method="POST" action="{{ route('admin.theory-courses.destroy',$course) }}" onsubmit="return confirm('Delete this course?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="7" class="text-center py-5">No theory courses found.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $courses->links() }}</div>@endsection
