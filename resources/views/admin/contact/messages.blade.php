@extends('layouts.admin')
@section('title','Messages') @section('page_title','Messages')
@section('content')<div class="page-heading mb-4"><h1>Contact Messages</h1><p>Messages submitted from the contact page.</p></div>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<div class="panel table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Name</th><th>Subject</th><th>Email / Phone</th><th>Date</th><th>Status</th><th></th></tr></thead><tbody>@forelse($messages as $message)<tr class="{{ $message->is_read?'':'fw-bold' }}"><td>{{ $message->name }}</td><td>{{ $message->subject }}</td><td>{{ $message->email }}<small class="d-block">{{ $message->phone }}</small></td><td>{{ $message->created_at->format('d M Y, h:i A') }}</td><td>{{ $message->is_read?'Read':'New' }}</td><td><a class="btn btn-sm btn-admin-primary" href="{{ route('admin.contact.messages.show',$message) }}">View</a></td></tr>@empty<tr><td colspan="6" class="text-center py-5">No messages found.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $messages->links() }}</div>@endsection
